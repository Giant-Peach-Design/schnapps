<picture class="block">
  {% if webp %}
    <source srcset="{{ webp }}" type="image/webp" />
  {% endif %}

  {% if url %}
    <img class="w-full !h-full object-cover" src="{{ url }}" alt="{{ alt }}" />
  {% endif %}
</picture>